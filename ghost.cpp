
#include <ncurses.h>
#include <iostream>
#include <time.h>
#include <vector>
#include "ghost.h"

//Rotate the ghost randomly when hit a wall
//Angle: 90 degrees
void ghost_speed() {
  vector<ghost> Ghosts;
  ghost gt1, gt2, gt3;
  gt1.speed = 1;
  gt2.speed = 1
  gt3.speed = 1;
  
  cin >> fastly;
  if      (fastly == "A")   { speed = 3; }
  else if (fastly == "B")   { speed = 5; }
  
}

void ghost_turn() {
  

